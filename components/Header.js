import React from 'react';

import {
    HeaderView,
    HeaderTitle
} from '../style/style'

const Header = () => {
    return(
        <HeaderView>
            <HeaderTitle>myBudgetApp</HeaderTitle>
        </HeaderView>
    );
}

export default Header;